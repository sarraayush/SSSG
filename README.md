# SSSG


---

## Tech Stack
- **Backend**: Node.js
- **Frontend**: React.js
- **Database**: MongoDB

---

## Pricing Calculation
For subscription pricing calculation, the following parameters are considered:
- Base Price: $10
- Price per Credit Line: $10
- Price per Credit Score Point: $10

---

## API Endpoints
- **File Upload**: `POST /file/upload`
  - Uploads a file to the server.
- **Get File**: `GET /file/get_file`
  - Retrieves a previously uploaded file from the server.

---

## Project Status
The backend API development has been successfully completed and is fully functional. 

---

## Setup Instructions
1. **Clone the Repository**: `git clone <repository-url>`
2. **Install Dependencies**: 
   - Navigate to the backend directory: `cd backend`
   - Install backend dependencies: `npm install`
   - Navigate to the frontend directory: `cd ../frontend`
   - Install frontend dependencies: `npm install`
3. **Configure Environment Variables**:
   - Backend: Create a `.env` file in the `backend` directory with the following variables:
     - `DB_CONNECTION`: MongoDB connection string.
     - `PORT`: Port number for the server.
4. **Run the Application**:
   - Backend: `npm start` in the `backend` directory.
   - Frontend: `npm start` in the `frontend` directory.

---

## Contact Information
For any inquiries or support regarding this project, please contact [Your Name](mailto:your-email@example.com).

---



Thank you for using our Data Driven Project! We hope it serves your needs effectively. Feel free to provide feedback and contribute to its improvement.
File generate using ChatGpt
